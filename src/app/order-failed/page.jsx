"use client";

import OrderFailed from "@/components/OrdersComponents/OrderFailed/OrderFailed";
import React from "react";

const page = () => {
    return (
        <div>
            <OrderFailed />
        </div>
    );
};

export default page;
